diff -ruN --no-dereference a/src/agents/dynamic-agent-core-sections.ts b/src/agents/dynamic-agent-core-sections.ts
--- a/src/agents/dynamic-agent-core-sections.ts	2026-04-11 20:10:10.462545808 +0900
+++ b/src/agents/dynamic-agent-core-sections.ts	2026-04-11 20:15:50.650375764 +0900
@@ -164,8 +164,8 @@
 - While waiting, only do non-overlapping prep work. Never ship implementation decisions Oracle was asked to decide.
 - Never "time out and continue anyway" for Oracle-dependent tasks.
 
-- Oracle takes minutes. When done with your own work: **end your response** - wait for the \`<system-reminder>\`.
-- Do NOT poll \`background_output\` on a running Oracle. The notification will come.
+- Oracle takes minutes. Use \`background_output(task_id="...", block=true)\` to wait for results.
+
 - Never cancel Oracle.
 </Oracle_Usage>`
 }
diff -ruN --no-dereference a/src/agents/dynamic-agent-policy-sections.ts b/src/agents/dynamic-agent-policy-sections.ts
--- a/src/agents/dynamic-agent-policy-sections.ts	2026-04-11 20:10:10.462563528 +0900
+++ b/src/agents/dynamic-agent-policy-sections.ts	2026-04-11 20:15:50.649261280 +0900
@@ -26,7 +26,7 @@
     '- **Testing**: Deleting failing tests to "pass"',
     "- **Search**: Firing agents for single-line typos or obvious syntax errors",
     "- **Debugging**: Shotgun debugging, random changes",
-    "- **Background Tasks**: Polling `background_output` on running tasks - end response and wait for notification",
+    "- **Background Tasks**: Not using `background_output(block=true)` when waiting for task results — always block-wait, do not rely on system notifications",
     "- **Delegation Duplication**: Delegating exploration to explore/librarian and then manually doing the same search yourself",
     "- **Oracle**: Delivering answer without collecting Oracle results",
   ]
diff -ruN --no-dereference a/src/agents/sisyphus/default.ts b/src/agents/sisyphus/default.ts
--- a/src/agents/sisyphus/default.ts	2026-04-11 20:10:10.463397094 +0900
+++ b/src/agents/sisyphus/default.ts	2026-04-11 20:15:50.650204995 +0900
@@ -331,10 +331,9 @@
 2. Continue only with non-overlapping work
    - If you have DIFFERENT independent work → do it now
    - Otherwise → **END YOUR RESPONSE.**
-3. **STOP. END YOUR RESPONSE.** The system will send \`<system-reminder>\` when tasks complete.
-4. On receiving \`<system-reminder>\` → collect results via \`background_output(task_id="...")\`
-5. **NEVER call \`background_output\` before receiving \`<system-reminder>\`.** This is a BLOCKING anti-pattern.
-6. Cleanup: Cancel disposable tasks individually via \`background_cancel(taskId="...")\`
+3. When waiting for results: call \`background_output(task_id="...", block=true)\` to actively wait until completion.
+4. Do NOT rely on system notifications to resume — use block=true to stay in the same turn.
+5. Cleanup: Cancel disposable tasks individually via \`background_cancel(taskId="...")\`
 
 ${buildAntiDuplicationSection()}
 
diff -ruN --no-dereference a/src/agents/sisyphus/gpt-5-4.ts b/src/agents/sisyphus/gpt-5-4.ts
--- a/src/agents/sisyphus/gpt-5-4.ts	2026-04-11 20:10:10.463443983 +0900
+++ b/src/agents/sisyphus/gpt-5-4.ts	2026-04-11 20:15:50.650249785 +0900
@@ -266,10 +266,9 @@
 2. Continue only with non-overlapping work
    - If you have DIFFERENT independent work → do it now
    - Otherwise → **END YOUR RESPONSE.**
-3. **STOP. END YOUR RESPONSE.** The system will send \`<system-reminder>\` when tasks complete.
-4. On receiving \`<system-reminder>\` → collect results via \`background_output(task_id="...")\`
-5. **NEVER call \`background_output\` before receiving \`<system-reminder>\`.** This is a BLOCKING anti-pattern.
-6. Cancel disposable tasks individually via \`background_cancel(taskId="...")\`
+3. When waiting for results: call \`background_output(task_id="...", block=true)\` to actively wait until completion.
+4. Do NOT rely on system notifications to resume — use block=true to stay in the same turn.
+5. Cleanup: Cancel disposable tasks individually via \`background_cancel(taskId="...")\`
 
 ${buildAntiDuplicationSection()}
 
diff -ruN --no-dereference a/src/agents/sisyphus.ts b/src/agents/sisyphus.ts
--- a/src/agents/sisyphus.ts	2026-04-11 20:10:10.463358364 +0900
+++ b/src/agents/sisyphus.ts	2026-04-11 20:15:50.650167226 +0900
@@ -258,10 +258,9 @@
 2. Continue only with non-overlapping work
    - If you have DIFFERENT independent work \u2192 do it now
    - Otherwise \u2192 **END YOUR RESPONSE.**
-3. **STOP. END YOUR RESPONSE.** The system will send \`<system-reminder>\` when tasks complete.
-4. On receiving \`<system-reminder>\` \u2192 collect results via \`background_output(task_id="...")\`
-5. **NEVER call \`background_output\` before receiving \`<system-reminder>\`.** This is a BLOCKING anti-pattern.
-6. Cleanup: Cancel disposable tasks individually via \`background_cancel(taskId="...")\`
+3. When waiting for results: call \`background_output(task_id="...", block=true)\` to actively wait until completion.
+4. Do NOT rely on system notifications to resume — use block=true to stay in the same turn.
+5. Cleanup: Cancel disposable tasks individually via \`background_cancel(taskId="...")\`
 
 ${buildAntiDuplicationSection()}
 
diff -ruN --no-dereference a/src/features/background-agent/manager.ts b/src/features/background-agent/manager.ts
--- a/src/features/background-agent/manager.ts	2026-04-11 20:10:10.467907180 +0900
+++ b/src/features/background-agent/manager.ts	2026-04-11 20:17:16.800962848 +0900
@@ -160,6 +160,8 @@
   private completedTaskSummaries: Map<string, BackgroundTaskNotificationTask[]> = new Map()
   private idleDeferralTimers: Map<string, ReturnType<typeof setTimeout>> = new Map()
   private notificationQueueByParent: Map<string, Promise<void>> = new Map()
+  private blockingTaskRefCounts: Map<string, number> = new Map()
+  private wasBlockedTasks: Set<string> = new Set()
   private observedOutputSessions: Set<string> = new Set()
   private observedIncompleteTodosBySession: Map<string, boolean> = new Map()
   private rootDescendantCounts: Map<string, number>
@@ -195,6 +197,25 @@
     this.registerProcessCleanup()
   }
 
+  registerBlockingTask(taskId: string): void {
+    const current = this.blockingTaskRefCounts.get(taskId) ?? 0
+    this.blockingTaskRefCounts.set(taskId, current + 1)
+    this.wasBlockedTasks.add(taskId)
+  }
+
+  unregisterBlockingTask(taskId: string): void {
+    const current = this.blockingTaskRefCounts.get(taskId) ?? 0
+    if (current <= 1) {
+      this.blockingTaskRefCounts.delete(taskId)
+    } else {
+      this.blockingTaskRefCounts.set(taskId, current - 1)
+    }
+  }
+
+  isTaskBlocking(taskId: string): boolean {
+    return (this.blockingTaskRefCounts.get(taskId) ?? 0) > 0 || this.wasBlockedTasks.has(taskId)
+  }
+
   private async abortSessionWithLogging(sessionID: string, reason: string): Promise<void> {
     try {
       await abortWithTimeout(this.client, sessionID)
@@ -1504,6 +1525,8 @@
 
       this.clearNotificationsForTask(taskId)
       this.tasks.delete(taskId)
+      this.wasBlockedTasks.delete(taskId)
+      this.blockingTaskRefCounts.delete(taskId)
       this.clearTaskHistoryWhenParentTasksGone(task.parentSessionID)
       if (task.sessionID) {
         subagentSessions.delete(task.sessionID)
@@ -1724,15 +1747,19 @@
       })
     }
 
-    if (!this.completedTaskSummaries.has(task.parentSessionID)) {
-      this.completedTaskSummaries.set(task.parentSessionID, [])
+    const shouldSuppressNotification = this.isTaskBlocking(task.id)
+
+    if (!shouldSuppressNotification) {
+      if (!this.completedTaskSummaries.has(task.parentSessionID)) {
+        this.completedTaskSummaries.set(task.parentSessionID, [])
+      }
+      this.completedTaskSummaries.get(task.parentSessionID)!.push({
+        id: task.id,
+        description: task.description,
+        status: task.status,
+        error: task.error,
+      })
     }
-    this.completedTaskSummaries.get(task.parentSessionID)!.push({
-      id: task.id,
-      description: task.description,
-      status: task.status,
-      error: task.error,
-    })
 
     // Update pending tracking and check if all tasks complete
     const pendingSet = this.pendingByParent.get(task.parentSessionID)
@@ -1753,7 +1780,7 @@
     }
 
     const completedTasks = allComplete
-      ? (this.completedTaskSummaries.get(task.parentSessionID) ?? [{ id: task.id, description: task.description, status: task.status, error: task.error }])
+      ? (this.completedTaskSummaries.get(task.parentSessionID) ?? [])
       : []
 
     if (allComplete) {
@@ -1767,21 +1794,23 @@
         : task.status === "error"
           ? "ERROR"
           : "CANCELLED"
-    const notification = buildBackgroundTaskNotificationText({
-      task,
-      duration,
-      statusText,
-      allComplete,
-      remainingCount,
-      completedTasks,
-    })
+    const notification: string | null = (!shouldSuppressNotification || (allComplete && completedTasks.length > 0))
+      ? buildBackgroundTaskNotificationText({
+        task,
+        duration,
+        statusText,
+        allComplete,
+        remainingCount,
+        completedTasks,
+      })
+      : null
 
       let agent: string | undefined = task.parentAgent
       let model: { providerID: string; modelID: string } | undefined
       let tools: Record<string, boolean> | undefined = task.parentTools
       let promptContext: ReturnType<typeof resolvePromptContextFromSessionMessages> = null
 
-      if (this.enableParentSessionNotifications) {
+      if (this.enableParentSessionNotifications && notification !== null) {
         try {
           const messagesResp = await this.client.session.messages({ path: { id: task.parentSessionID } })
           const messages = normalizeSDKResponse(messagesResp, [] as Array<{
@@ -1868,13 +1897,21 @@
             log("[background-agent] Failed to send notification:", error)
           }
         }
-      } else {
+      } else if (!this.enableParentSessionNotifications) {
         log("[background-agent] Parent session notifications disabled, skipping prompt injection:", {
           taskId: task.id,
           parentSessionID: task.parentSessionID,
         })
       }
 
+      if (shouldSuppressNotification) {
+        log("[background-agent] Cleaned up blocking state for task collected via block=true:", {
+          taskId: task.id,
+          notificationSent: notification !== null,
+        })
+        this.wasBlockedTasks.delete(task.id)
+      }
+
     if (task.status !== "running" && task.status !== "pending") {
       this.scheduleTaskRemoval(task.id)
     }
@@ -2058,6 +2095,7 @@
         const completionSource = sessionStatus?.type === "idle"
           ? "polling (idle status)"
           : "polling (session gone from status)"
+
         const hasValidOutput = await this.validateSessionHasOutput(sessionID)
         if (!hasValidOutput) {
           if (sessionGoneThresholdReached) {
@@ -2179,6 +2217,8 @@
     this.processingKeys.clear()
     this.taskHistory.clearAll()
     this.completedTaskSummaries.clear()
+    this.blockingTaskRefCounts.clear()
+    this.wasBlockedTasks.clear()
     this.unregisterProcessCleanup()
     log("[background-agent] Shutdown complete")
 
diff -ruN --no-dereference a/src/tools/background-task/clients.ts b/src/tools/background-task/clients.ts
--- a/src/tools/background-task/clients.ts	2026-04-11 20:10:10.491952611 +0900
+++ b/src/tools/background-task/clients.ts	2026-04-11 20:15:50.675571665 +0900
@@ -29,4 +29,4 @@
   }
 }
 
-export type BackgroundOutputManager = Pick<BackgroundManager, "getTask">
+export type BackgroundOutputManager = Pick<BackgroundManager, "getTask" | "registerBlockingTask" | "unregisterBlockingTask">
diff -ruN --no-dereference a/src/tools/background-task/constants.ts b/src/tools/background-task/constants.ts
--- a/src/tools/background-task/constants.ts	2026-04-11 20:10:10.491967081 +0900
+++ b/src/tools/background-task/constants.ts	2026-04-11 20:15:50.675257916 +0900
@@ -2,8 +2,6 @@
 
 Use \`background_output\` to get results. Prompts MUST be in English.`
 
-export const BACKGROUND_OUTPUT_DESCRIPTION = `Get output from background task. Use full_session=true to fetch session messages with filters. System notifies on completion, so block=true rarely needed. - Timeout values are in milliseconds (ms), NOT seconds.
-
-IMPORTANT: ONLY call this tool AFTER receiving a <system-reminder> notification for the task. Do NOT call immediately after launching a background task - wait for the notification first.`
+export const BACKGROUND_OUTPUT_DESCRIPTION = `Get output from background task. Use full_session=true to fetch session messages with filters. When waiting for agent results, use block=true to actively wait rather than relying on system notifications.`
 
 export const BACKGROUND_CANCEL_DESCRIPTION = `Cancel running background task(s). Use all=true to cancel ALL before final answer.`
diff -ruN --no-dereference a/src/tools/background-task/create-background-output.blocking.test.ts b/src/tools/background-task/create-background-output.blocking.test.ts
--- a/src/tools/background-task/create-background-output.blocking.test.ts	2026-04-11 20:10:10.491997991 +0900
+++ b/src/tools/background-task/create-background-output.blocking.test.ts	2026-04-11 20:15:50.675556275 +0900
@@ -58,6 +58,8 @@
 
         return task
       },
+      registerBlockingTask: () => {},
+      unregisterBlockingTask: () => {},
     }
 
     const tool = createBackgroundOutput(manager, createMockClient())
@@ -79,7 +81,7 @@
     expect(output).not.toContain("Timed out waiting")
   })
 
-  test("returns legacy status output with timeout note when task stays running", async () => {
+  test("waits indefinitely until task completes when block=true (timeout ignored)", async () => {
     // #given
     let pollCount = 0
     const task = createTask({ status: "running" })
@@ -87,8 +89,13 @@
       getTask: (id: string) => {
         if (id !== task.id) return undefined
         pollCount += 1
+        if (pollCount >= 3) {
+          task.status = "completed"
+        }
         return task
       },
+      registerBlockingTask: () => {},
+      unregisterBlockingTask: () => {},
     }
 
     const tool = createBackgroundOutput(manager, createMockClient())
@@ -104,9 +111,7 @@
     )
 
     // #then
-    expect(pollCount).toBeGreaterThanOrEqual(2)
-    expect(output).toContain("# Task Status")
-    expect(output).toContain("Timed out waiting")
-    expect(output).toContain("still running")
+    expect(pollCount).toBeGreaterThanOrEqual(3)
+    expect(output).not.toContain("Timed out waiting")
   })
 })
diff -ruN --no-dereference a/src/tools/background-task/create-background-output.metadata.test.ts b/src/tools/background-task/create-background-output.metadata.test.ts
--- a/src/tools/background-task/create-background-output.metadata.test.ts	2026-04-11 20:10:10.492012451 +0900
+++ b/src/tools/background-task/create-background-output.metadata.test.ts	2026-04-11 20:15:50.675291086 +0900
@@ -30,6 +30,8 @@
     }
     const manager: BackgroundOutputManager = {
       getTask: id => (id === task.id ? task : undefined),
+      registerBlockingTask: () => {},
+      unregisterBlockingTask: () => {},
     }
     const client: BackgroundOutputClient = {
       session: {
diff -ruN --no-dereference a/src/tools/background-task/create-background-output.ts b/src/tools/background-task/create-background-output.ts
--- a/src/tools/background-task/create-background-output.ts	2026-04-11 20:10:10.492027080 +0900
+++ b/src/tools/background-task/create-background-output.ts	2026-04-11 20:15:50.675241346 +0900
@@ -39,10 +39,6 @@
   return status === "pending" || status === "running"
 }
 
-function appendTimeoutNote(output: string, timeoutMs: number): string {
-  return `${output}\n\n> **Timed out waiting** after ${timeoutMs}ms. Task is still running; showing latest available output.`
-}
-
 export function createBackgroundOutput(manager: BackgroundOutputManager, client: BackgroundOutputClient): ToolDefinition {
   return tool({
     description: BACKGROUND_OUTPUT_DESCRIPTION,
@@ -52,9 +48,9 @@
         .boolean()
         .optional()
         .describe(
-          "Wait for completion (default: false). System notifies when done, so blocking is rarely needed."
+          "Wait for completion (default: false). When waiting for agent results, use block=true to actively wait rather than relying on system notifications."
         ),
-      timeout: tool.schema.number().optional().describe("Max wait time in ms (default: 60000, max: 600000)"),
+      timeout: tool.schema.number().optional().describe("Deprecated: block=true now waits indefinitely. This parameter is ignored."),
       full_session: tool.schema.boolean().optional().describe("Return full session messages with filters (default: false)"),
       include_thinking: tool.schema.boolean().optional().describe("Include thinking/reasoning parts in full_session output (default: false)"),
       message_limit: tool.schema.number().optional().describe("Max messages to return (capped at 100)"),
@@ -88,38 +84,28 @@
         }
 
         const shouldBlock = args.block === true
-        const timeoutMs = Math.min(args.timeout ?? 60000, 600000)
 
         let resolvedTask = task
 
-        let didTimeoutWhileActive = false
-
         if (shouldBlock && isTaskActiveStatus(task.status)) {
-          const startTime = Date.now()
-          while (Date.now() - startTime < timeoutMs) {
-            await delay(1000)
-
-            const currentTask = manager.getTask(args.task_id)
-            if (!currentTask) {
-              return `Task was deleted: ${args.task_id}`
-            }
-
-            resolvedTask = currentTask
-
-            if (!isTaskActiveStatus(currentTask.status)) {
-              break
+          manager.registerBlockingTask(args.task_id)
+          try {
+            while (true) {
+              await delay(1000)
+
+              const currentTask = manager.getTask(args.task_id)
+              if (!currentTask) {
+                return `Task was deleted: ${args.task_id}`
+              }
+
+              resolvedTask = currentTask
+
+              if (!isTaskActiveStatus(currentTask.status)) {
+                break
+              }
             }
-          }
-
-          if (isTaskActiveStatus(resolvedTask.status)) {
-            const finalCheck = manager.getTask(args.task_id)
-            if (finalCheck) {
-              resolvedTask = finalCheck
-            }
-          }
-
-          if (isTaskActiveStatus(resolvedTask.status)) {
-            didTimeoutWhileActive = true
+          } finally {
+            manager.unregisterBlockingTask(args.task_id)
           }
         }
 
@@ -137,7 +123,7 @@
             thinkingMaxChars: args.thinking_max_chars,
           })
 
-          return didTimeoutWhileActive ? appendTimeoutNote(output, timeoutMs) : output
+          return output
         }
 
         if (resolvedTask.status === "completed") {
@@ -150,7 +136,7 @@
         }
 
         const statusOutput = formatTaskStatus(resolvedTask)
-        return didTimeoutWhileActive ? appendTimeoutNote(statusOutput, timeoutMs) : statusOutput
+        return statusOutput
       } catch (error) {
         return `Error getting output: ${error instanceof Error ? error.message : String(error)}`
       }
diff -ruN --no-dereference a/src/tools/background-task/create-background-output.undo.test.ts b/src/tools/background-task/create-background-output.undo.test.ts
--- a/src/tools/background-task/create-background-output.undo.test.ts	2026-04-11 20:10:10.492044330 +0900
+++ b/src/tools/background-task/create-background-output.undo.test.ts	2026-04-11 20:15:50.675305446 +0900
@@ -91,6 +91,8 @@
     const task = createTask()
     const manager: BackgroundOutputManager = {
       getTask: id => (id === task.id ? task : undefined),
+      registerBlockingTask: () => {},
+      unregisterBlockingTask: () => {},
     }
     const tool = createBackgroundOutput(manager, createMockClient())
     const eventHandler = createMockEventHandler()
diff -ruN --no-dereference a/src/tools/background-task/create-background-task.ts b/src/tools/background-task/create-background-task.ts
--- a/src/tools/background-task/create-background-task.ts	2026-04-11 20:10:10.492091910 +0900
+++ b/src/tools/background-task/create-background-task.ts	2026-04-11 20:15:50.675385346 +0900
@@ -114,9 +114,7 @@
 Agent: ${task.agent}
 Status: ${task.status}
 
-System notifies on completion. Use \`background_output\` with task_id="${task.id}" to check.
-
-Do NOT call background_output now. Wait for <system-reminder> notification first.`
+Use \`background_output\` with task_id="${task.id}" and block=true to wait for results.`
       } catch (error) {
         const message = error instanceof Error ? error.message : String(error)
         return `[ERROR] Failed to launch background task: ${message}`
diff -ruN --no-dereference a/src/tools/background-task/tools.test.ts b/src/tools/background-task/tools.test.ts
--- a/src/tools/background-task/tools.test.ts	2026-04-11 20:10:10.492234919 +0900
+++ b/src/tools/background-task/tools.test.ts	2026-04-11 20:15:50.675335396 +0900
@@ -23,6 +23,8 @@
 function createMockManager(task: BackgroundTask): BackgroundOutputManager {
   return {
     getTask: (id: string) => (id === task.id ? task : undefined),
+    registerBlockingTask: () => {},
+    unregisterBlockingTask: () => {},
   }
 }
 
@@ -355,6 +357,8 @@
         }
         return task
       },
+      registerBlockingTask: () => {},
+      unregisterBlockingTask: () => {},
     }
     const client = createMockClient({
       "ses-blocking-default": [
diff -ruN --no-dereference a/src/tools/background-task/types.ts b/src/tools/background-task/types.ts
--- a/src/tools/background-task/types.ts	2026-04-11 20:10:10.492285279 +0900
+++ b/src/tools/background-task/types.ts	2026-04-11 20:15:50.675320126 +0900
@@ -47,7 +47,7 @@
   }
 }
 
-export type BackgroundOutputManager = Pick<import("../../features/background-agent").BackgroundManager, "getTask">
+export type BackgroundOutputManager = Pick<import("../../features/background-agent").BackgroundManager, "getTask" | "registerBlockingTask" | "unregisterBlockingTask">
 
 export type FullSessionMessagePart = {
   type?: string
diff -ruN --no-dereference a/src/tools/call-omo-agent/background-agent-executor.ts b/src/tools/call-omo-agent/background-agent-executor.ts
--- a/src/tools/call-omo-agent/background-agent-executor.ts	2026-04-11 20:10:10.492354109 +0900
+++ b/src/tools/call-omo-agent/background-agent-executor.ts	2026-04-11 20:15:50.675640754 +0900
@@ -81,9 +81,7 @@
 Agent: ${task.agent} (subagent)
 Status: ${task.status}
 
-System notifies on completion. Use \`background_output\` with task_id="${task.id}" to check.
-
-Do NOT call background_output now. Wait for <system-reminder> notification first.`
+Use \`background_output\` with task_id="${task.id}" and block=true to wait for results.`
 	} catch (error) {
 		const message = error instanceof Error ? error.message : String(error)
 		return `Failed to launch background agent task: ${message}`
diff -ruN --no-dereference a/src/tools/call-omo-agent/background-executor.ts b/src/tools/call-omo-agent/background-executor.ts
--- a/src/tools/call-omo-agent/background-executor.ts	2026-04-11 20:10:10.492385688 +0900
+++ b/src/tools/call-omo-agent/background-executor.ts	2026-04-11 20:15:50.675676634 +0900
@@ -88,9 +88,7 @@
 Agent: ${task.agent} (subagent)
 Status: ${task.status}
 
-System notifies on completion. Use \`background_output\` with task_id="${task.id}" to check.
-
-Do NOT call background_output now. Wait for <system-reminder> notification first.`
+Use \`background_output\` with task_id="${task.id}" and block=true to wait for results.`
   } catch (error) {
     const message = error instanceof Error ? error.message : String(error)
     return `Failed to launch background agent task: ${message}`
diff -ruN --no-dereference a/src/features/background-agent/background-task-notification-template.ts b/src/features/background-agent/background-task-notification-template.ts
--- a/src/features/background-agent/background-task-notification-template.ts	2026-04-12 00:00:00.000000000 +0900
+++ b/src/features/background-agent/background-task-notification-template.ts	2026-04-12 00:00:00.000000000 +0900
@@ -66,9 +66,8 @@
 **Description:** ${safeDescription(task)}
 **Duration:** ${duration}${errorInfo}
 
-**${remainingCount} task${remainingCount === 1 ? "" : "s"} still in progress.** You WILL be notified when ALL complete.
-${isFailure ? "**ACTION REQUIRED:** This task failed. Check the error and decide whether to retry, cancel remaining tasks, or continue." : "Do NOT poll - continue productive work."}
-
+**${remainingCount} task${remainingCount === 1 ? "" : "s"} still in progress.** Use \`background_output(task_id="...", block=true)\` to wait for remaining results.
+${isFailure ? "\n**ACTION REQUIRED:** This task failed. Check the error and decide whether to retry, cancel remaining tasks, or continue.\n" : ""}
 Use \`background_output(task_id="${task.id}")\` to retrieve this result when ready.
 </system-reminder>`
 }
diff -ruN --no-dereference a/src/tools/delegate-task/background-task.ts b/src/tools/delegate-task/background-task.ts
--- a/src/tools/delegate-task/background-task.ts	2026-04-12 00:00:00.000000000 +0900
+++ b/src/tools/delegate-task/background-task.ts	2026-04-12 00:00:00.000000000 +0900
@@ -151,9 +151,7 @@
 Agent: ${task.agent}${args.category ? ` (category: ${args.category})` : ""}
 Status: ${task.status}
 
-System notifies on completion. Use \`background_output\` with task_id="${task.id}" to check.
-
-Do NOT call background_output now. Wait for <system-reminder> notification first.${taskMetadataBlock}`
+Use \`background_output\` with task_id="${task.id}" and block=true to wait for results.${taskMetadataBlock}`
   } catch (error) {
     return formatDetailedError(error, {
       operation: "Launch background task",
diff -ruN --no-dereference a/src/tools/delegate-task/background-continuation.ts b/src/tools/delegate-task/background-continuation.ts
--- a/src/tools/delegate-task/background-continuation.ts	2026-04-12 00:00:00.000000000 +0900
+++ b/src/tools/delegate-task/background-continuation.ts	2026-04-12 00:00:00.000000000 +0900
@@ -51,9 +51,7 @@
 Status: ${task.status}
 
 Agent continues with full previous context preserved.
-System notifies on completion. Use \`background_output\` with task_id="${task.id}" to check.
-
-Do NOT call background_output now. Wait for <system-reminder> notification first.
+Use \`background_output\` with task_id="${task.id}" and block=true to wait for results.
 
 <task_metadata>
 session_id: ${task.sessionID}
