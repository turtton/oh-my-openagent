--- a/src/features/background-agent/session-idle-event-handler.ts	2026-04-11 20:16:19.848722238 +0900
+++ b/src/features/background-agent/session-idle-event-handler.ts	2026-04-11 20:14:21.219821130 +0900
@@ -13,6 +13,7 @@
   idleDeferralTimers: Map<string, ReturnType<typeof setTimeout>>
   validateSessionHasOutput: (sessionID: string) => Promise<boolean>
   checkSessionTodos: (sessionID: string) => Promise<boolean>
+  hasActiveBlockWaiter: (taskId: string) => boolean
   tryCompleteTask: (task: BackgroundTask, source: string) => Promise<boolean>
   emitIdleEvent: (sessionID: string) => void
 }): void {
@@ -22,6 +23,7 @@
     idleDeferralTimers,
     validateSessionHasOutput,
     checkSessionTodos,
+    hasActiveBlockWaiter,
     tryCompleteTask,
     emitIdleEvent,
   } = args
@@ -55,6 +57,19 @@
     return
   }
 
+  const taskIsBlocking = hasActiveBlockWaiter(task.id)
+
+  if (taskIsBlocking) {
+    // Parent is actively waiting with block=true - skip output validation and todo checks,
+    // complete immediately so the blocking loop can return results.
+    log("[background-agent] Task is blocking, skipping guards for immediate completion:", task.id)
+    tryCompleteTask(task, "session.idle event (blocking bypass)")
+      .catch((err) => {
+        log("[background-agent] Error in blocking bypass completion:", err)
+      })
+    return
+  }
+
   validateSessionHasOutput(sessionID)
     .then(async (hasValidOutput) => {
       if (task.status !== "running") {
--- a/src/features/background-agent/manager.ts	2026-04-11 20:17:16.800962848 +0900
+++ b/src/features/background-agent/manager.ts	2026-04-11 20:14:21.220009819 +0900
@@ -212,8 +212,12 @@
     }
   }
 
+  hasActiveBlockWaiter(taskId: string): boolean {
+    return (this.blockingTaskRefCounts.get(taskId) ?? 0) > 0
+  }
+
   isTaskBlocking(taskId: string): boolean {
-    return (this.blockingTaskRefCounts.get(taskId) ?? 0) > 0 || this.wasBlockedTasks.has(taskId)
+    return this.hasActiveBlockWaiter(taskId) || this.wasBlockedTasks.has(taskId)
   }
 
   private async abortSessionWithLogging(sessionID: string, reason: string): Promise<void> {
@@ -1148,6 +1152,7 @@
         idleDeferralTimers: this.idleDeferralTimers,
         validateSessionHasOutput: (id) => this.validateSessionHasOutput(id),
         checkSessionTodos: (id) => this.checkSessionTodos(id),
+        hasActiveBlockWaiter: (taskId) => this.hasActiveBlockWaiter(taskId),
         tryCompleteTask: (task, source) => this.tryCompleteTask(task, source),
         emitIdleEvent: (sessionID) => this.handleEvent({ type: "session.idle", properties: { sessionID } }),
       })
@@ -2096,6 +2101,20 @@
           ? "polling (idle status)"
           : "polling (session gone from status)"
 
+        if (this.hasActiveBlockWaiter(task.id)) {
+          // Still check for crashed sessions even when bypassing output/todo guards
+          if (sessionGoneThresholdReached) {
+            const sessionExists = await this.verifySessionExists(sessionID)
+            if (!sessionExists) {
+              log("[background-agent] Session crashed during block=true wait:", task.id)
+              await this.failCrashedTask(task, "Subagent session no longer exists (process likely crashed). The session disappeared without producing any output.")
+              continue
+            }
+          }
+          await this.tryCompleteTask(task, `${completionSource} (blocking bypass)`)
+          continue
+        }
+
         const hasValidOutput = await this.validateSessionHasOutput(sessionID)
         if (!hasValidOutput) {
           if (sessionGoneThresholdReached) {
